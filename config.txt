;********************************************************************************

; Number of bits per data, Uint: bit
dataWidth 64

; Number of bits per data segment, Uint: bit
dataWidthSegment 8

; Write mode
WMode M_out_of_N_Write

; Number of data per racetrack	
NDR 1

;********************************************************************************
